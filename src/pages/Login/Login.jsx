import { LoginForm } from "../../components/LoginForm/LoginForm";

import "./Login.css";

export const Login = () => {
  return (
    <section className="login-section">
      <LoginForm />
    </section>
  );
};
